#ifndef _ep004_h_
#define _ep004_h_

// test if integer is palindromic
bool IsPali(int number);

// find number of digits in an integer
int IntLen(int number);

// separate digits in an integer
int* IntSplit(int number, int length);

#endif
